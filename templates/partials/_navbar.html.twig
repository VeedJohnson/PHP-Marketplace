
<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">Ads Center</a>
        <div class="d-flex align-items-center gap-3">
{#            <div class="position-relative">#}
{#                <i class="bi bi-geo-alt-fill text-primary"></i>#}
{#                <span class="ms-1">San Francisco, CA</span>#}
{#            </div>#}
            {% if app.user %}
                <div class="dropdown">
                    <button class="btn btn-info rounded-circle"  id="userDropdown" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <span class="fw-bold">{{ app.user.firstName|slice(0, 1) }}{{ app.user.lastName|slice(0, 1) }}</span>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                        <li><a class="dropdown-item" href="{{ path('app_profile') }}"><i class="bi bi-person me-2"></i>Profile</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_advertisement_new') }}"><i class="bi bi-plus-circle me-2"></i>Post an Ad</a></li>
                        {# In your navbar template #}
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a class="dropdown-item" href="{{ path('app_admin_dashboard') }}">
                                    <i class="bi bi-shield-lock me-2"></i>Admin Dashboard
                                </a></li>
                        {% endif %}
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
                    </ul>
                </div>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-primary">Login</a>
            {% endif %}
        </div>
    </div>
</nav>
